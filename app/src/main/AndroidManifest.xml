<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sebax.miapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registro" />
        <activity android:name=".Usuario" />
        <activity android:name=".Presidenciales" />
        <activity android:name=".Diputados" />
        <activity android:name=".Senadores" />
        <activity android:name=".Core" />
        <activity android:name=".MuestraVoto" />
        <activity android:name=".Senador" />
        <activity android:name=".S" />
        <activity android:name=".Cores"></activity>
    </application>

</manifest>